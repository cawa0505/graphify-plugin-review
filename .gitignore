# Rust build
target

# Local-only files (never pushed to GitHub)
AGENTS.md
LOCAL-NOTES.md

# Upstream reference clone (tirth8205/code-review-graph) — reference only
github-sourcecode/

# Local runtime state
.code-review-graph/
.cortexkit/
